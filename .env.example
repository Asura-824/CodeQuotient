# Environment Configuration
NODE_ENV=production # Change to 'production' when deploying

# Server Configuration
PORT=3000
HOST=0.0.0.0 # Listen on all network interfaces

# MongoDB Configuration
MONGODB_URI=mongodb+srv://MyAppUser:Shubhamsk123@cluster0.p6girvj.mongodb.net/userDB?appName=Cluster0
MONGODB_DB_NAME=userDB

# Session Configuration
SESSION_SECRET=[your-strong-secret] # Change this in production!
SESSION_TTL=86400 # Session duration in seconds (24 hours)

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,https://shubhams-codequotient.netlify.app
CLIENT_ORIGIN=https://shubhams-codequotient.netlify.app

# Security Configuration
ENABLE_HTTPS=true # Set to true in production
TRUST_PROXY=1 # Required for reverse proxies
COOKIE_SECURE=true # Set to true in production
COOKIE_SAMESITE=none # Use 'lax' in development, 'none' in production

# Rate Limiting
RATE_LIMIT_WINDOW=15 # Time window in minutes
RATE_LIMIT_MAX=100 # Max requests per window